spring:
  application:
      name: elk
  datasource:
    driver-class-name: com.informix.jdbc.IfxDriver
    username: cloud_sys
    password: ylz2018
    url: jdbc:informix-sqli://192.168.44.86:9088/cloud_sys:INFORMIXSERVER=gbaseserver;DB_LOCALE=zh_CN.utf8;CLIENT_LOCALE=zh_CN.utf8;NEWCODESET=UTF8,utf8,57372;GL_USEGLU=1
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  http:
    encoding:
      force: true
  data:
    elasticsearch:
      cluster-name: ylz
      cluster-nodes: 192.168.44.88:9300

mybatis:
  mapper-locations: classpath:mapper/*.xml
  config-location: classpath:config/mybatis-conf.xml

logging:
  level:
    com.ylz.log.elk.manage.dao.mapper: debug

feign:
  hystrix:
    enabled: true